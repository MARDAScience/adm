#conda env create --file auto_ortho_masker.yml
#conda activate aom

name: aom
channels:
  - conda-forge
  - defaults
dependencies:
 - python
 - scipy
 - numpy
 - scikit-image
 - tensorflow-gpu
 - ipython
 - rasterio
 - joblib
 - tqdm
